# Foto previa — mig 336 (21-ago-2026)

Tomada **ANTES** de aplicar `336_rpc_bot_validan_parametros.sql`, para comparar contra sí misma.

🔴 **No comparar contra números escritos en la migración: el inventario se mueve todas las
noches.** Entre el 20 y el 21-ago la venta pasó de 383 a 385 sin que nadie tocara nada. Por eso
la verificación es antes/después **en la misma sesión**, sin una captura nocturna en el medio.

## La query

```sql
SELECT (resumen_mercado('venta')   ->'general'->>'total')::int   AS venta_total,
       (resumen_mercado('venta')   ->'general'->>'mediana')::int AS venta_mediana,
       (resumen_mercado('alquiler')->'general'->>'total')::int   AS alq_total,
       (resumen_mercado('alquiler')->'general'->>'mediana')::int AS alq_mediana,
       jsonb_array_length(buscar_propiedades('venta'))           AS lista_venta,
       jsonb_array_length(buscar_propiedades('alquiler'))        AS lista_alq,
       (buscar_propiedades('venta')->0->>'id')                   AS primera_venta_id,
       (buscar_propiedades('alquiler')->0->>'id')                AS primera_alq_id;
```

## El resultado ANTES

| campo | valor |
|---|---|
| `venta_total` | **385** |
| `venta_mediana` | **97000** |
| `alq_total` | **196** |
| `alq_mediana` | **4500** |
| `lista_venta` | **6** |
| `lista_alq` | **6** |
| `primera_venta_id` | **1120** |
| `primera_alq_id` | **3787** |

🔑 Van **medianas e ids de la primera fila**, no solo conteos: un conteo igual no prueba que el
orden ni el filtro se mantuvieron. Si `primera_venta_id` cambia, el `ORDER BY` se movió — y ese
es justo el riesgo de haber reemplazado `p_orden` por `v_orden` dentro del `CASE`.

## Después de aplicar

Correr la misma query. **Los 8 valores deben ser idénticos.** Cualquier diferencia = la migración
movió algo que no debía; revertir con
`336_ROLLBACK_rpc_bot_validan_parametros.sql`.
